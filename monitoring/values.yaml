alertmanager:
  tplConfig: true

  stringConfig: |
    global:
      smtp_smarthost: "smtp.gmail.com:587"
      smtp_from: "purvaa462@gmail.com"
      smtp_auth_username: "purvaa462@gmail.com"
      smtp_auth_password: "trdr ewka dfpm gmou"
      smtp_require_tls: true

    route:
      receiver: gmail-notifications
      group_wait: 30s
      group_interval: 5m
      repeat_interval: 1h

      routes:
        - matchers:
            - alertname="Watchdog"
          receiver: "null"

        - matchers:
            - alertname="KubeControllerManagerDown"
          receiver: "null"

        - matchers:
            - alertname="KubeSchedulerDown"
          receiver: "null"

        - matchers:
            - alertname="KubeProxyDown"
          receiver: "null"

    receivers:
      - name: "null"

      - name: gmail-notifications
        email_configs:
          - to: "purvaa462@gmail.com"
            send_resolved: true