apiVersion: v1
kind: Service
metadata:
  name: deploysafe-service
spec:
  type: NodePort
  selector:
    app: deploysafe
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30007
